sdk.zip
temp_jdk/
temp_sdk/
wrapper/
gradle/
build/
app/build/
.gradle/
build_log.txt
Copy of presupuesto 2.5.xlsx
create_fake_excel.py
local.properties

# Secrets / credentials (NEVER commit)
service-account.json
*-service-account.json
serviceAccountKey.json
*.pem
*.p12
*.keystore
*.jks
# API keys locales (la app las lee de DataStore; esto es solo respaldo local)
secrets.local.properties
*.local.secret

# Heap dumps
*.hprof

# Root-level generated DB (the canonical seed DB lives in app/src/main/assets/)
/budget_database.db

# ETL artifacts
etl_output.log

# UI verification screenshots (transient) y JSON crudo del import de diseño
ui_reference/screenshots/
ui_reference/claude_design/*.raw.json
.kotlin/
Copy of presupuesto 2.5 (1).xlsx
scripts/etl/__pycache__/
