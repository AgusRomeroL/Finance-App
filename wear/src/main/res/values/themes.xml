<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base de Wear OS: fondo negro, sin ActionBar. Compose pinta el resto. -->
    <style name="Theme.BudgetWear" parent="@android:style/Theme.DeviceDefault">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
